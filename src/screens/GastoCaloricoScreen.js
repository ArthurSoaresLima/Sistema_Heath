import { View,Text, Button, TextInput } from "react-native";

export default function GastoCaloricoScreen({navigation}){
    return(
        <View>
            <Text >Tela GastoCaloricoScreen</Text>
        </View>
    )
}